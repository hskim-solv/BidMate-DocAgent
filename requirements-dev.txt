# Development and verification dependencies for Agentic-VLM.
# Keep this file focused on tools needed for local/Codex validation.

pytest>=9.0.3
# Coverage measurement for scripts/test.sh + pr-eval CI job (issue #323).
# Surfaces coverage % in GitHub Actions step summary and uploads coverage.xml
# artifact; README Codecov badge consumes the same XML.
pytest-cov>=5.0
# Required by fastapi.testclient (starlette.testclient) for tests/test_api.py.
httpx>=0.28.1,<1.0
# Opt-in OpenAI embedding backend (issue #161, embedding ablation runner).
# Production runtime does NOT import openai — only contributors running
# scripts/run_embedding_ablation.py --models text-embedding-3-large need this.
openai>=2.36.0
# Opt-in cross-encoder reranker backends (issue #163, rag_rerank.py).
# CI default is BIDMATE_RERANK_BACKEND=stub (identity, no SDK needed).
# Real backends — BGE-reranker-v2-m3 / dragonkue-ko via FlagEmbedding, or
# Cohere rerank-3.5-multilingual — are opt-in only.
FlagEmbedding>=1.2
cohere>=5.0
